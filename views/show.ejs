<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Views by id</title>
    <style>
        /* General Styles */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #121212, #1e1e1e);
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            padding: 20px;
        }

        h2 {
            font-size: 2rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
        }

        p {
            font-size: 1.2rem;
            color: #bbb;
            margin-bottom: 10px;
        }

        .post {
            background: rgba(255, 255, 255, 0.1); /* Glass effect */
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            padding: 20px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
            margin-bottom: 20px;
        }

        .post h3 {
            font-size: 1.5rem;
            margin: 10px 0;
            color: #f0f0f0;
            text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.7);
        }

        .post h3:first-of-type {
            color: #00aaff;
        }

        a {
            font-size: 1.2rem;
            text-decoration: none;
            color: #fff;
            background: linear-gradient(135deg, #00aaff, #0055ff);
            padding: 10px 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            transition: background 0.3s ease, transform 0.2s ease;
        }

        a:hover {
            background: linear-gradient(135deg, #0055ff, #00aaff);
            transform: scale(1.05);
        }

        a:active {
            transform: scale(1);
        }
    </style>
</head>
<body>
    <h2>Here the details more</h2>
    <p>Post id <%=post.id%></p>
    <div class="post">
        <h3><%=post.username%></h3>
        <h3><%=post.content%></h3>
    </div>
    <a href="http://localhost:8080/">Back to all posts</a>
</body>
</html>